type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Mockup PSD | Free Download",
  site_description: "Starter mockup template for design with eymockup",
  site_domain: "https://next-wp.com",
};
